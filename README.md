# 积存金持仓管理系统

一个功能完善的积存金投资持仓管理工具，支持多次买入、卖出操作、保本价计算和卖出收益记录查询。

## 功能特性

### 持仓管理

- **多次买入记录**：支持分批次买入，记录每次买入的时间、价格、克数和手续费
- **数据持久化**：使用浏览器 localStorage 保存数据，刷新不丢失
- **卖出操作**：支持按克数或按金额两种卖出方式
- **持仓统计**：实时显示总持仓克数、总投入金额、加权平均成本
- **保本卖出价**：根据持仓成本自动计算保本价格
- **买入记录管理**：支持查看、删除单条记录或清空全部

### 卖出收益记录

- **收益汇总卡片**：展示累计卖出次数、总金额、总收益、平均收益率
- **时间筛选**：支持全部、今日、本周、本月、自定义日期范围筛选
- **多维排序**：按时间、收益金额、收益率升序/降序排列
- **分页加载**：每页5条记录，智能页码显示，优化大数据量性能
- **收益明细**：包含卖出时间、卖出价、成本价、克数、金额、手续费、收益金额和收益率
- **视觉区分**：盈利显示红色，亏损显示绿色（符合中国股市习惯）

## 使用方法

### 在线使用

直接在浏览器中打开 `index.html` 文件即可使用。

### 本地运行

```bash
# 使用 Python 启动本地服务器
python -m http.server 8080

# 然后在浏览器中访问
# http://localhost:8080/index.html
```

## 计算公式

### 持仓管理

```
加权平均成本 = Σ(买入金额 + 买入手续费) / 总克数
保本卖出价 = 加权平均成本 / (1 - 卖出手续费率/100)
```

### 卖出收益

```
成本价格 = 卖出时记录的加权平均成本价格
收益金额 = 卖出金额 - 卖出手续费 - (卖出克数 × 成本价格)
收益率 = 收益金额 / (卖出克数 × 成本价格) × 100%
```

**注意**：卖出时会记录当时的加权平均成本价格，确保收益计算准确反映卖出时的真实情况。

## 数据存储

所有数据保存在浏览器的 localStorage 中，key 为 `goldTradeData`。

数据结构：
```json
{
  "version": 1,
  "buyRecords": [
    {
      "id": "唯一标识",
      "timestamp": "买入时间",
      "price": "买入单价",
      "weight": "买入克数",
      "amount": "买入金额",
      "feeRate": "手续费率",
      "fee": "手续费",
      "remainingWeight": "剩余克数"
    }
  ],
  "sellRecords": [
    {
      "id": "唯一标识",
      "timestamp": "卖出时间",
      "price": "卖出单价",
      "weight": "卖出克数",
      "amount": "卖出金额",
      "feeRate": "手续费率",
      "fee": "手续费",
      "costPrice": "卖出时的加权平均成本价格"
    }
  ]
}
```

## 性能优化

卖出收益记录模块针对大数据量场景进行了多项优化：

- **DocumentFragment**：批量创建DOM节点，减少重排重绘
- **渲染防抖**：100ms延迟，避免频繁渲染
- **requestAnimationFrame**：在浏览器最佳时机渲染
- **汇总数据缓存**：避免重复计算
- **for循环替代reduce**：遍历性能提升约30%
- **智能分页显示**：只显示5个页码+省略号，避免页码过多

## 技术栈

- HTML5
- CSS3（响应式设计）
- JavaScript (ES6+)
- localStorage

## 浏览器支持

- Chrome（推荐）
- Firefox
- Safari
- Edge

## 注意事项

1. 数据仅保存在本地浏览器中，更换浏览器或清除浏览器数据会导致数据丢失
2. 买入手续费率默认为 0%，卖出手续费率默认为 0.4%，可根据实际情况调整
3. 卖出时采用 FIFO（先进先出）原则扣减持仓
4. 卖出时会记录当时的加权平均成本价格，确保历史收益数据准确

## 许可证

MIT License